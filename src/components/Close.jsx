import { IconButton } from '@mui/material'
import CloseRoundedIcon from '@mui/icons-material/CloseRounded'

export default function Close() {
  return (
    <IconButton title="Fermer le menu">
      <CloseRoundedIcon />
    </IconButton>
  )
}
